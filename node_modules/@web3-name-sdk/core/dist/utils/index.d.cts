import { Address } from 'viem';
export { namehash } from 'viem';

declare function labelhash(unnormalizedLabelOrLabelhash: string): `0x${string}`;

declare const normalize: (name: string) => string;
declare function tldNamehash(inputName: string, identifier?: bigint): Address;

declare function validateName(name: string): string;

export { labelhash, normalize, tldNamehash, validateName };
